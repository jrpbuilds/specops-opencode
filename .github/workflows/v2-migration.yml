name: OpenCode 2 migration

on:
    push:
        branches:
            - beta

permissions:
    contents: write

jobs:
    check:
        runs-on: ubuntu-latest
        steps:
            - name: Check out beta
              uses: actions/checkout@v4
              with:
                  ref: beta
            - name: Set up Bun
              uses: oven-sh/setup-bun@v2
              with:
                  bun-version: 1.3.14
            - name: Resolve pinned OpenCode 2 dependencies
              run: bun install --frozen-lockfile
            - name: Snapshot pinned plugin type contract
              run: |
                  rm -rf .migration/opencode-plugin-contract
                  mkdir -p .migration/opencode-plugin-contract
                  plugin_root="$(dirname "$(realpath node_modules/@opencode-ai/plugin/package.json)")"
                  cp "$plugin_root/package.json" .migration/opencode-plugin-contract/package.json
                  find "$plugin_root/dist" -type f -name '*.d.ts' -print0 | while IFS= read -r -d '' file; do
                      rel="${file#$plugin_root/}"
                      mkdir -p ".migration/opencode-plugin-contract/$(dirname "$rel")"
                      cp "$file" ".migration/opencode-plugin-contract/$rel"
                  done
            - name: Commit contract snapshot when changed
              run: |
                  git config user.name github-actions[bot]
                  git config user.email 41898282+github-actions[bot]@users.noreply.github.com
                  git add .migration/opencode-plugin-contract
                  if git diff --cached --quiet; then
                      exit 0
                  fi
                  git commit -m "chore(beta): refresh pinned OpenCode 2 contract"
                  git push origin HEAD:beta
