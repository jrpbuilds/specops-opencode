Carry every Reviewer FAIL finding `F1..Fn` and correction target verbatim; do not summarize, paraphrase, renumber, or drop any. Fix root causes within approved scope and verify each finding independently, even when one fix addresses several. Lane and candidate IDs are round-scoped evidence, not canonical findings.

After remediation, reset the prior review round and re-apply the `## Review phase` gate. Choose a fresh proportionate plan — direct, focused, full, or expanded under `reviewFanout` — from current code, remediation changes, outstanding canonical findings, and regression risk. Prior lanes inform coverage, not assignments: retain, reshape, add, or drop scoped lanes and capability hints as surfaces change. Never drop coverage to evade independent verification of an unresolved concern; broad or risky fixes may still need full or expanded review.

Run selected lanes with fresh critics on current code, then re-dispatch `specops-reviewer` for a full re-review with new reports (if any), the remediation summary, prior findings verbatim, and an explicit re-review instruction. The Final Reviewer independently rechecks every prior finding, the complete approved change, regressions, and new material approved-scope defects regardless of lane composition; resolved findings must not distract from remaining defects.
